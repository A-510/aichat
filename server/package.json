{
  "name": "aichat",
  "version": "1.0.0",
  "description": "AI Chat 应用",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd server && node index.js"
  },
  "dependencies": {}
}