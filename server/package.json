{
  "name": "aichat",
  "version": "1.0.0",
  "description": "AI Chat 应用",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd server && node index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  }
}